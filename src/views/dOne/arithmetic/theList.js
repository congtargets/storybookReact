/**
 * 链表的实现
 */

const { appendFile } = require("fs-extra");

// 创建节点
class ListNode{
    constructor(data){
        this.data = data;
        this.next =null;
    }
}

// 追加节点
append(data){
    let lsitNode = new ListNode(data);
    if(this)
}
console.log(ListNode);